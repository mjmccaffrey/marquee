
make back button longpress? => reboot

change print to logging; include timestamps

* problem on 3/19 with Output turning off on 3.1 and 5.1.
* * * add output=on to all commands?

* dimmer.restart
* ? special in mode 2, restart the dimmers, and re-init the objects?

* am I overriding 'function' ??
* Must set dimmers to 100 upon mode switch -- "baseline"
* finalize preset stuff
* clean up Build N/EQ

GPIO 4 blown on Pi zero ?

Timeline
========
21 Mar - release to github
31 Mar - video to github


To Do Now:
    GPIO terminal block
    Pi zero 2
    Install remote; 2 buttons functional

    check and respond to .get result codes
    pace must measure between the same 2 points, not blindly wait
        AND TAKE SPEED_FACTOR PROPERLY INTO ACCOUNT
        is this needed on RPi zero 2?

After checking in as fully functional release:
    Install lights 11 and 12

    error handling for I/O

    modes:
        dimmer pattern sequences
        Spin, or other action, as all bulbs slowly build
        relay version of mode_random_fade(player: Player, transition=None)
        fade even & odd lights at opposite phases
        Each light hits peak in counter/clockwise order

Physical:
    Caps for top holes
    New front face
    procure QR code signs

Future:
    A   
        light_sequence + relay_override + set_lights ==>
        make_dimmer_sequence(light_sequence) + parameters + set_dimmers
    B
        native dimmer sequence + parameters + set_dimmers
    
    dim 100 - 0 w/ trans=0.5, but interrupt at 0.25
        player.add_mode_func(24, "dimmer_rotate_trick", mode=lambda: dimmer_rotate_trick(player))

        def dimmer_rotate_trick(player: Player):

            player.sign.set_dimmers(WILL THIS TAKE A PATTERN AND A TRANSITION PARM?)
            trans=0.5
            pace=0.25

        def seq_dimmer_rotate(
                pattern="A" + "5" * (LIGHT_COUNT - 2) + "0", 
                clockwise=True,
        ):
            """Rotate a dimmer pattern of lights counter/clockwise."""
            if clockwise:
                light_range = range(LIGHT_COUNT, 0, -1)
            else:  # counterclockwise
                light_range = range(0, LIGHT_COUNT, 1)
            for i in light_range:
                rotated_pattern = pattern[i:] + pattern[:i]
                yield rotated_pattern

    create small doc listing power supplies (including tripp lite) capacities and attached loads

    command to configure dimmers

    tcp/ip setup working off home network
    12 lights: Sides fade and build, and then top & bottom do the same
    logging module
    use type_enforced
    public and private modes
    reboot_dimmers command
    New modes:
        During a mode, slow down relays (pace) while dimming, speed up while building
        Rather than a fixed transition rate, calculate so that effective rate is 10%-20% per second
        Rows of bulbs progressively fade, and then build back from the bottom
        Blink_all_fade_seq with rotating order - build+0, fade+1, build+2, fade+3, etc.
        New build mode:
            row 4 = 100
            row 3 = 50, 4 = 80
            row 2 = 50, 4 = 70
            row 1 = 50, 4 = 50
    basic testing and code coverage
    completely replace requests with aiohttp?
        https://docs.aiohttp.org/en/stable/http_request_lifecycle.html
    https://realpython.com/python-pyproject-toml/


log and retry upon dimmer comm timeouts

Create requirements.txt

rythmic demo overhaul
