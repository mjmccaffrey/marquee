
. Update dimmer firmware

Create requirements.txt

Bugfix
- Restore command-line examples

Feature:
_ specify brightness on command line: 0123456789A
_ fade even & odd lights at opposite phases
_ Each light hits peak in counter/clockwise order
_ Indicate more than 10 modes




    def _brightness_pattern_to_percentages(pattern):
        """ """

        try:
            return [int(p, 16) for p in pattern]
        

        result = ""
        for b in pattern.upcase():
            if b in "0123456789":
                result += int(b) * 10
            elif b == "A":
                result += "100"
            else:
                raise ValueError(b)
                